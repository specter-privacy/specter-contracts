# Foundry build artifacts
/out
/cache
/broadcast
/artifacts

# Dependencies (tracked via git submodules / forge install)
# Do NOT ignore /lib — submodule refs belong in git

# Coverage output
lcov.info
coverage/

# Environment — never commit secrets
.env
.env.*
!.env.example

# Forge script cache
/cache/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Node (if any frontend tooling is added)
node_modules/
